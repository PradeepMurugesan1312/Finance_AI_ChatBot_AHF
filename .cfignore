.git/
.venv/
venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.mypy_cache/
tests/
htmlcov/
.coverage
scratchpad/
droplet*/
*.tgz
*.droplet
.env
joule-capability/
scripts/
*.daar
Images for Claude/
# Built at container start (KB_REBUILD_ON_START) from knowledge_base/docs/ — a
# stale local copy would just be overwritten, so don't bother shipping it.
knowledge_base/index.json

# NOTE: do not exclude README.md — pyproject.toml's `readme = "README.md"` is
# read by the build backend during `pip install .` at staging time, and a
# missing file fails the whole deploy with a metadata-generation error.
