[project]
name = "ahf-finance-agent"
version = "0.1.0"
description = "AHF Finance AI ChatBot — read-only A2A agent for AP / procurement / finance FAQs, grounded in company documents, escalates to a human when not confident."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    # A2A protocol server. Pinned to the exact version proven end-to-end in
    # this Joule tenant by the sibling agent (ap_inquiry_agent). Do not bump
    # without re-testing the full Joule round trip.
    "a2a-sdk[http-server]==0.3.26",
    "httpx>=0.27.0",
    # SAP AI Core GPT deployments expose an OpenAI-compatible inference API.
    # We call it with the openai SDK directly (not generative-ai-hub-sdk, which
    # pins an older pydantic that conflicts with a2a-sdk).
    "openai>=1.40.0",
    "pydantic>=2.11.3",
    "pydantic-settings>=2.4.0",
    "sse-starlette>=3.4.8",
    "starlette>=1.6.0",
    "uvicorn>=0.30.0",
]

[project.optional-dependencies]
# step 3 (S/4HANA tool orchestration) is a hand-rolled tool-calling loop on the
# openai SDK (see ahf_finance_agent/answering.py) — no langgraph/langchain, to
# keep the dependency surface small and the loop fully unit-testable offline.
# step 4 (RAG / policy KB): the `local` vector index in
# ahf_finance_agent/knowledge_base.py is pure stdlib + httpx (already a dep) —
# no new dependencies. The `hana` backend (SAP HANA Cloud vector store) is a
# stub; wiring it up later adds `hdbcli` here.

[project.scripts]
ahf-finance-agent = "ahf_finance_agent:main"

[dependency-groups]
dev = [
    "pytest>=8.0.0",
    "pyyaml>=6.0",
]

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"

[tool.uv.build-backend]
module-name = "ahf_finance_agent"
module-root = "src"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
